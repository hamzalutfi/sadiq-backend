const sendEmail = async ({ email, subject, template, data }) => {
  // This is a stub. Integrate with nodemailer or any email service provider.
  // Use the template and data to render the email body.
  return Promise.resolve();
};

module.exports = { sendEmail }; 